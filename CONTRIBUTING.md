# Contributing

Use Node 20+, run `npm ci`, then `make verify`. Keep the domain core deterministic and add a regression test for every corrected behavior. Do not include real rider data, credentials, or unlicensed feeds.
