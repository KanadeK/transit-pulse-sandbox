# Privacy and security

The bundled examples are synthetic. Imported files are parsed locally in the browser and are not uploaded. The application has no telemetry, authentication, remote map tiles, or external API dependency. Treat imported schedules as potentially sensitive and avoid committing them.
